_schema-version: "2.0.0"
ID: "ztest"
version: 1.0.0

parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "ZTEST"
    type: zip
    path: .
    parameters:
       version: 1.0.0
    build-parameters:
      builder: grunt
      build-result: dist